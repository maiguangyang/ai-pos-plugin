name: easypos_demo_app
description: Standalone runnable shell for the EasyPOS demo integration.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.8.1"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  easypos_demo_integration:
    path: ../../packages/easypos_demo_integration

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
