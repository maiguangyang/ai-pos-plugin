name: ai_pos_plugin_api
description: Stable public contracts for compile-time AI POS integrations.
version: 0.3.0
publish_to: "none"

environment:
  sdk: ">=3.8.1"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
