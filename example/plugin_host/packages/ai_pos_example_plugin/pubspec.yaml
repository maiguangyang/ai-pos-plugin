name: ai_pos_example_plugin
description: Reference third-party package for the AI POS plugin contract.
version: 0.3.0
publish_to: "none"

environment:
  sdk: ">=3.8.1"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  ai_pos_plugin_api:
    path: ../../../../core/ai_pos_plugin_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
